import { describe, expect, it } from "vitest";

import { mount } from '@vue/test-utils'
import Board from '@/components/Board.vue'

describe('test Board', () => {
  it("should show SDK Management", () => {})
})
